development:
  clients:
    default:
      database: secrets-service-development
      hosts:
        - localhost:27017
      options:
        user: "secrets-service"
        password: "secret"
        auth_source: admin
        auth_mech: :scram
test:
  clients:
    default:
      database: secrets-service-test
      hosts:
        - localhost:27017
      options:
        user: "secrets-service"
        password: "secret"
        auth_source: admin
        auth_mech: :scram
