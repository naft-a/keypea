development:
  clients:
    default:
      database: auth-service-development
      hosts:
        - localhost:27017
      options:
        user: "auth-service"
        password: "secret"
        auth_source: admin
        auth_mech: :scram
test:
  clients:
    default:
      database: auth-service-test
      hosts:
        - localhost:27017
      options:
        user: "auth-service"
        password: "secret"
        auth_source: admin
        auth_mech: :scram
